<div class="subscribe">
  <p>Get notified of new chapters!</p>

  <form id="subscribe-form">
    <input
      type="email"
      id="email"
      placeholder="Email address"
      required
    />

    <button type="submit">
      Subscribe
    </button>
  </form>
  <div class="subscribe-message">
  <p id="subscribe-message" style="font-size: 16px;"></p></div>
  </div>

<script is:inline>
  const form = document.getElementById("subscribe-form");
  const emailInput = document.getElementById("email");
  const message = document.getElementById("subscribe-message");

  form.addEventListener("submit", async (event) => {
    event.preventDefault();

    message.textContent = "Subscribing...";

    try {
      const response = await fetch(
        "https://berriezai-newsletter.dxxzaiamvs.workers.dev/subscribe",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify({
            email: emailInput.value
          })
        }
      );

      if (response.ok) {
        message.textContent = "You're subscribed!";
        form.reset();
      } else if (response.status === 409) {
        message.textContent = "You're already subscribed!";
      } else {
        message.textContent = "Something went wrong. Please try again.";
}

    } catch (error) {
      message.textContent = "Could not connect. Please try again later.";
    }
  });
</script>